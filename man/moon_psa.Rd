% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moon-psa.R
\name{moon_psa}
\alias{moon_psa}
\title{Run a probabilistic sensitivity analysis (PSA) on a MOON spec}
\usage{
moon_psa(
  spec,
  n_iter,
  seed = NULL,
  parallel = FALSE,
  store_traces = c("summary", "all", "none"),
  correlate_hr = TRUE,
  correlate_cost = TRUE,
  tp_overrides = NULL
)
}
\arguments{
\item{spec}{A spec'd \code{params} list, typically from
\code{moon_params_norway(sex = ..., uncertainty = TRUE)}.}

\item{n_iter}{Integer; number of PSA iterations. The published MOON
analysis uses \code{1000}.}

\item{seed}{Integer (or \code{NULL}); passed to \code{\link[=moon_sample_params]{moon_sample_params()}} so the
draw sequence is reproducible.}

\item{parallel}{Logical. \code{FALSE} (default) runs iterations sequentially
with \code{lapply}. \code{TRUE} uses \code{furrr::future_map} with a multicore
(Linux / macOS) or multisession (Windows) plan; falls back to
sequential with a warning if \code{furrr} / \code{future} are unavailable.}

\item{store_traces}{One of \code{"summary"} (default — keep a 3-D array
\verb{[iter, age, state]} of trace counts), \code{"all"} (keep the full list of
\code{moon_deterministic} objects; large), or \code{"none"} (drop traces; keep
only summary, per-iter metrics, and draws).}

\item{correlate_hr, correlate_cost}{Logical, passed to
\code{\link[=moon_sample_params]{moon_sample_params()}}; both default \code{TRUE}.}

\item{tp_overrides}{Optional, forwarded to every \code{\link[=moon_deterministic]{moon_deterministic()}}
call. Use to run a PSA on a scenario rather than the base case.}
}
\value{
A \code{moon_psa} S3 object — a list with
\itemize{
\item \code{summary} — per \verb{(sex, metric)} summary
(mean, 2.5 / 97.5 quantile, sd).
\item \code{per_iter} — long data frame of per-iteration metric values.
\item \code{traces} — depends on \code{store_traces}.
\item \code{draws} — long table of sampled mortality HRs by
\verb{(iter, parameter)}.
\item \code{params_spec} — the input spec, preserved for replay.
\item \code{meta} — run metadata.
}
}
\description{
Materialises \code{n_iter} parameter draws from a spec built with
\code{moon_params_norway(uncertainty = TRUE)}, runs \code{\link[=moon_deterministic]{moon_deterministic()}} on
each, computes per-iteration metrics, and aggregates them into a
\code{moon_psa} object.
}
\examples{
\donttest{
spec <- moon_params_norway(sex = "female", uncertainty = TRUE)
psa  <- moon_psa(spec, n_iter = 50, seed = 1)
head(psa$summary)
}

}
\seealso{
\code{\link[=moon_deterministic]{moon_deterministic()}}, \code{\link[=moon_params_norway]{moon_params_norway()}},
\code{\link[=moon_sample_params]{moon_sample_params()}}.
}
