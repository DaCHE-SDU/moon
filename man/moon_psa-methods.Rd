% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moon-psa-methods.R
\name{moon_psa-methods}
\alias{moon_psa-methods}
\alias{print.moon_psa}
\alias{summary.moon_psa}
\alias{as.data.frame.moon_psa}
\alias{plot.moon_psa}
\title{Methods for \code{moon_psa} objects}
\usage{
\method{print}{moon_psa}(x, ...)

\method{summary}{moon_psa}(object, ...)

\method{as.data.frame}{moon_psa}(
  x,
  row.names = NULL,
  optional = FALSE,
  what = c("per_iter", "draws", "summary"),
  ...
)

\method{plot}{moon_psa}(x, type = c("forest", "incremental_cost_age"), ...)
}
\arguments{
\item{x, object}{A \code{\link[=moon_psa]{moon_psa()}} object.}

\item{...}{Further arguments passed to or from other methods.}

\item{row.names, optional}{Standard \code{as.data.frame()} arguments;
currently ignored.}

\item{what}{For \code{as.data.frame()}, one of \code{"per_iter"} (default),
\code{"draws"}, or \code{"summary"} — selects which long-form data frame to
return.}

\item{type}{For \code{plot()}, one of \code{"forest"} (default; point + 95\% CI
per metric, faceted by metric family) or \code{"incremental_cost_age"}
(per-age incremental cost vs NW, mean +/- 95\% band; requires
\code{store_traces = "all"} on the original \code{\link[=moon_psa]{moon_psa()}} call).}
}
\value{
\code{print()} returns its input invisibly (and prints headline
metric bands). \code{summary()} returns the precomputed summary data frame
(mean, 95\% CI, sd per \verb{(sex, metric)}). \code{as.data.frame()} returns the
requested long data frame. \code{plot()} returns a \code{ggplot} object; the
\code{ggplot2} package is required.
}
\description{
S3 methods for inspecting, summarising, plotting, and coercing the
object returned by \code{\link[=moon_psa]{moon_psa()}}.
}
\seealso{
\code{\link[=moon_psa]{moon_psa()}}, \code{\link[=moon_deterministic]{moon_deterministic()}}.
}
