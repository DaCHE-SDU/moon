% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moon-deterministic-extractors.R
\name{moon_costs}
\alias{moon_costs}
\title{Aggregate cohort costs by age, state, sex, or total}
\usage{
moon_costs(
  x,
  by = c("age", "state", "sex", "total"),
  discounted = FALSE,
  ages = NULL
)
}
\arguments{
\item{x}{A \code{\link[=moon_deterministic]{moon_deterministic()}} object.}

\item{by}{One of \code{"age"}, \code{"state"}, \code{"sex"}, \code{"total"}. \code{"total"}
returns a length-1 numeric (the grand total).}

\item{discounted}{\code{FALSE} (default) sums \verb{$cost}; \code{TRUE} sums
\verb{$cost_disc}.}

\item{ages}{Optional integer vector to filter; \code{NULL} (default) returns
all ages.}
}
\value{
A data frame with columns \verb{<by>} and \code{cost}, except for
\code{by = "total"} which returns a length-1 numeric.
}
\description{
Aggregates the \verb{$costs} slot of a \code{\link[=moon_deterministic]{moon_deterministic()}} object over a
single grouping variable. For multi-variable groupings, run twice or
build your own \code{aggregate()} call against
\code{as.data.frame(x, what = "costs")}.
}
\examples{
\donttest{
params <- moon_params_norway(sex = "female")
run    <- moon_deterministic(params)
moon_costs(run, by = "state")
moon_costs(run, by = "total", discounted = TRUE)
}

}
\seealso{
\code{\link[=moon_deterministic]{moon_deterministic()}}, \code{\link[=moon_prevalence]{moon_prevalence()}}.
}
