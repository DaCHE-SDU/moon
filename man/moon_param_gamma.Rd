% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moon-params.R
\name{moon_param_gamma}
\alias{moon_param_gamma}
\title{Moment-matched gamma spec from per-cell means and standard errors}
\usage{
moon_param_gamma(mean_vec, se_vec)
}
\arguments{
\item{mean_vec}{Non-negative numeric vector of per-cell means.}

\item{se_vec}{Non-negative numeric vector of per-cell standard errors,
the same length as \code{mean_vec}.}
}
\value{
A \code{moon_param_gamma} spec object.
}
\description{
Used for per-capita health-care costs. Each cell uses a gamma with
\code{shape = (mean / se)^2} and \code{scale = se^2 / mean}. Degenerate cells where
\code{mean == 0} or \code{se == 0} (e.g. ages 2–19 in the bundled cost CSVs) are
returned as deterministic 0 rather than NaN.
}
\examples{
g <- moon_param_gamma(mean_vec = c(0, 100, 250), se_vec = c(0, 30, 60))
moon_param_value(g)
moon_param_sample(g, n = 5)

}
\seealso{
\link{moon_param-methods}, \code{\link[=moon_param_fixed]{moon_param_fixed()}},
\code{\link[=moon_param_lognormal]{moon_param_lognormal()}}, \code{\link[=moon_param_mvnorm]{moon_param_mvnorm()}}.
}
