% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moon-deterministic-methods.R
\name{moon_deterministic-methods}
\alias{moon_deterministic-methods}
\alias{print.moon_deterministic}
\alias{summary.moon_deterministic}
\alias{print.summary.moon_deterministic}
\alias{as.data.frame.moon_deterministic}
\alias{plot.moon_deterministic}
\title{Methods for \code{moon_deterministic} objects}
\usage{
\method{print}{moon_deterministic}(x, ...)

\method{summary}{moon_deterministic}(object, ...)

\method{print}{summary.moon_deterministic}(x, ...)

\method{as.data.frame}{moon_deterministic}(
  x,
  row.names = NULL,
  optional = FALSE,
  what = c("trace", "costs"),
  ...
)

\method{plot}{moon_deterministic}(
  x,
  type = c("occupancy", "prevalence_alive", "prevalence_initial", "survival", "costs"),
  ages = NULL,
  by_sex = FALSE,
  ...
)
}
\arguments{
\item{x, object}{A \code{\link[=moon_deterministic]{moon_deterministic()}} object (or, for
\code{print.summary.moon_deterministic}, the object returned by
\code{summary()}).}

\item{...}{Further arguments passed to or from other methods.}

\item{row.names, optional}{Standard \code{as.data.frame()} arguments;
currently ignored.}

\item{what}{For \code{as.data.frame()}, one of \code{"trace"} (default) or
\code{"costs"} — selects which long-form data frame to return.}

\item{type}{For \code{plot()}, one of \code{"occupancy"}, \code{"prevalence_alive"},
\code{"prevalence_initial"}, \code{"survival"}, or \code{"costs"}.}

\item{ages}{For \code{plot()}, optional integer vector to filter the
underlying trace and costs to before plotting; \code{NULL} (default) plots
all ages.}

\item{by_sex}{Forward-compatibility hook for multi-sex stitched runs;
has no effect today since each \code{moon_deterministic} object is
single-sex by construction.}
}
\value{
\code{print()} returns its input invisibly (and prints a one-screen
headline). \code{summary()} returns a \code{summary.moon_deterministic} object
carrying life-expectancy, total/discounted cost, age-45 prevalence,
and per-state per-capita incremental cost vs NW. \code{as.data.frame()}
returns the requested long data frame. \code{plot()} returns a \code{ggplot}
object; the \code{ggplot2} package is required.
}
\description{
S3 methods for inspecting, summarising, plotting, and coercing the object
returned by \code{\link[=moon_deterministic]{moon_deterministic()}}.
}
\seealso{
\code{\link[=moon_deterministic]{moon_deterministic()}}, \code{\link[=moon_prevalence]{moon_prevalence()}},
\code{\link[=moon_costs]{moon_costs()}}.
}
