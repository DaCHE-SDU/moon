% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moon-sample-params.R
\name{moon_sample_params}
\alias{moon_sample_params}
\title{Materialise a spec'd \code{params} list into \code{n} concrete draws}
\usage{
moon_sample_params(
  spec,
  n,
  seed = NULL,
  correlate_hr = TRUE,
  correlate_cost = TRUE
)
}
\arguments{
\item{spec}{A spec'd \code{params} list, typically from
\code{moon_params_norway(uncertainty = TRUE)}.}

\item{n}{Integer; number of iterations to materialise.}

\item{seed}{Integer (or \code{NULL}); calls \code{set.seed(seed)} before drawing
so the resulting list is reproducible.}

\item{correlate_hr, correlate_cost}{Logical; see Details. Both default
to \code{TRUE} to match the published analysis.}
}
\value{
A length-\code{n} list of plain-value \code{params} lists, each ready for
\code{\link[=moon_deterministic]{moon_deterministic()}}.
}
\description{
Walks a spec'd \code{params} list (typically from
\code{moon_params_norway(uncertainty = TRUE)}) and materialises \code{n} fully
concrete \code{params} lists ready for \code{\link[=moon_deterministic]{moon_deterministic()}}.
}
\details{
Two correlation flags reproduce the published MOON conventions:
\itemize{
\item \code{correlate_hr} (default \code{TRUE}) — one \code{rnorm(n)} draw is reused as the
Z-source for all 9 mortality-HR lognormals (3 states × 3 age bands).
\item \code{correlate_cost} (default \code{TRUE}) — one \code{runif(n)} draw is reused as
the inverse-CDF input for every cost gamma.
}

When a flag is \code{FALSE}, each spec generates its own independent draws.
Within a single call (with a fixed \code{seed}) the RNG order is HR \code{z} →
cost \code{u} → transition-coefficient mvnorm draws (in order of band-key).
The order is stable across calls but is not expected to match the legacy
MOON port byte-for-byte.
}
\examples{
\donttest{
spec  <- moon_params_norway(sex = "female", uncertainty = TRUE)
draws <- moon_sample_params(spec, n = 5, seed = 1)
length(draws)
}

}
\seealso{
\code{\link[=moon_params_norway]{moon_params_norway()}}, \code{\link[=moon_psa]{moon_psa()}},
\code{\link[=moon_deterministic]{moon_deterministic()}}.
}
