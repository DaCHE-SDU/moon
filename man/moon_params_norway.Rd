% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params-norway.R
\name{moon_params_norway}
\alias{moon_params_norway}
\title{Build a Norwegian default \code{params} list}
\usage{
moon_params_norway(
  sex = c("female", "male", "both"),
  uncertainty = FALSE,
  data_dir = system.file("extdata", package = "moon", mustWork = TRUE),
  lifetable_file = "Lifetable_Norway_2017.csv"
)
}
\arguments{
\item{sex}{One of \code{"female"}, \code{"male"}, \code{"both"}. Selects which CSV
file family to load and which life-table column to use as \code{qx}.}

\item{uncertainty}{If \code{FALSE} (default), returns plain numeric values
ready for \code{\link[=moon_deterministic]{moon_deterministic()}}. If \code{TRUE}, wraps the uncertain
inputs (mortality hazard ratios, costs, transition coefficients) as
\verb{moon_param_*} spec objects ready for \code{\link[=moon_sample_params]{moon_sample_params()}} /
\code{\link[=moon_psa]{moon_psa()}}. The result is \strong{not} valid input for
\code{\link[=moon_deterministic]{moon_deterministic()}} directly — sample it first.}

\item{data_dir}{Directory containing the parameter CSVs. Defaults to
\code{system.file("extdata", package = "moon", mustWork = TRUE)}, which
resolves to the bundled files inside the installed package; override
to point at a custom directory of CSVs with the same filenames and
schema.}

\item{lifetable_file}{File name (within \code{data_dir}) of the life-table
CSV. Defaults to \code{"Lifetable_Norway_2017.csv"} to match the bundled
data; override when supplying a life table for a different
country / year. The CSV must have an \code{Age} column and a sex-keyed
column matching the \code{sex} argument (\code{F}, \code{M}, or \code{Both}).}
}
\value{
A \code{params} list with elements \code{start_age}, \code{max_age},
\code{discount_rate}, \code{cost_currency}, \code{cohort_n}, \code{init_prev}, \code{qx},
\code{mortality_hr}, \code{transition_probs}, \code{cost_df}. Under
\code{uncertainty = TRUE} the same shape, with the uncertain slots replaced
by \verb{moon_param_*} specs:
\itemize{
\item \code{mortality_hr$OW} / \code{OB1} / \code{OB2} become list-columns of
\code{\link[=moon_param_lognormal]{moon_param_lognormal()}} specs.
\item \code{cost_df$cost} becomes a list of \code{\link[=moon_param_gamma]{moon_param_gamma()}} specs (one per
\verb{(state, age)} cell; degenerate ages 2–19 get gammas with
\code{mean = se = 0} which always sample 0).
\item \code{transition_probs} becomes a list with \verb{$specs} (named
\code{\link[=moon_param_mvnorm]{moon_param_mvnorm()}} objects keyed by
\code{"<transition>_<age_start>_<age_end>"}) and \verb{$bands} (the schema for
stitching per-band draws back into the deterministic
\code{transition_probs} layout).
}
}
\description{
Constructs the engine-shape \code{params} list for the published MOON
Norwegian birth cohort (Bjørnelv et al. 2021), reading the bundled CSVs
for life tables, costs, and survival-model coefficients. The returned
list plugs straight into \code{\link[=moon_deterministic]{moon_deterministic()}} (or \code{\link[=moon_sample_params]{moon_sample_params()}}
/ \code{\link[=moon_psa]{moon_psa()}} when \code{uncertainty = TRUE}) with no further reshape.
}
\details{
Single-sex per call. \code{cohort_n} is a length-1 named integer
(e.g. \code{c(female = 26458L)}) so the stratification label is recoverable
from the params alone.
}
\section{Cost data ranges}{


The bundled cost CSVs carry \strong{zero} values for ages 2–19 and are \strong{held
constant at the age-80 value for ages 81–100}. This reflects the
original two-part regression's fitted age range of 20–80
(Bjørnelv et al. 2021, §Health care costs; Supplementary Appendix 4
tables s8–s15) — the published cost predictions only cover ages 20–80,
so the engine extends them flat at the boundaries. These are pre-existing
data choices baked into the bundled parameter files, not engine-side
imputations.
}

\section{\code{init_prev} is keyed to \code{start_age}}{


\code{init_prev} is the BMI distribution \strong{at age \code{start_age}}, despite the
field name not saying so. The bundled value is the published age-2
distribution and pairs with the bundled \code{start_age = 2L}. If you mutate
\code{start_age} to a later age, the stored \code{init_prev} will silently
produce wrong results unless you also supply a matched prevalence
vector for that age — \code{\link[=moon_check_params]{moon_check_params()}} cannot detect this, since
there is no external truth to check against.

See \code{vignette("moon-customizing", package = "moon")} §6 for a
bootstrap recipe (run the baseline, read prevalence at the target age
out of \code{\link[=moon_prevalence]{moon_prevalence()}}, feed it back as \code{init_prev}).
}

\examples{
\donttest{
params <- moon_params_norway(sex = "female")
str(params, max.level = 1)
}

}
\seealso{
\code{\link[=moon_deterministic]{moon_deterministic()}}, \code{\link[=moon_sample_params]{moon_sample_params()}},
\code{\link[=moon_check_params]{moon_check_params()}}.
}
