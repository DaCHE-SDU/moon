% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{run_markov_engine}
\alias{run_markov_engine}
\title{Run the raw MOON Markov engine}
\usage{
run_markov_engine(
  start_age,
  max_age,
  discount_rate,
  init_prev,
  transition_probs,
  qx,
  mortality_hr,
  cost_df,
  tp_overrides = NULL
)
}
\arguments{
\item{start_age}{Integer; first model age (e.g. \code{2}).}

\item{max_age}{Integer; one past the last model age (e.g. \code{100}).
\code{n_cycles = max_age - start_age}.}

\item{discount_rate}{Numeric in \verb{[0, 1)}; applied to \code{cost_matrix} only.}

\item{init_prev}{Named numeric \code{c(NW = ..., OW = ..., OB1 = ..., OB2 = ...)}
summing to 1. All \code{NW} mass enters the engine as \code{N_always}; \code{N_prev}
starts at 0.}

\item{transition_probs}{Data frame with columns \verb{age, NW_OW, OW_OB1, OB1_OB2, OW_NW, OB1_OW, OB2_OB1} — one row per age in
\code{start_age:(max_age - 1)}.}

\item{qx}{Named numeric vector of NW baseline mortality probabilities;
names are ages as character.}

\item{mortality_hr}{Data frame with columns \verb{age_lower, OW, OB1, OB2}
(three rows, one per age band 35 / 50 / 70).}

\item{cost_df}{Data frame with columns \verb{age, state}
(\code{NW} / \code{OW} / \code{OB1} / \code{OB2}), \code{cost}. Single-sex, already filtered.
\code{NULL} or zero-row for no costs.}

\item{tp_overrides}{Optional list. Supported slots: \code{set_zero} (character
vector of transitions to zero out), \code{init_prev} (overrides
\code{init_prev}), \code{start_age} (overrides \code{start_age}).}
}
\value{
A list with
\itemize{
\item \code{trace} — \verb{(n_cycles + 1) x 6} matrix of state proportions
(rows sum to 1).
\item \code{cost_matrix} — \verb{(n_cycles + 1) x 4} matrix of per-capita costs by
\verb{(age, state)}.
\item \code{mortality} — list of mortality probability vectors per state
(\code{NW}, \code{OW}, \code{OB1}, \code{OB2}).
}
}
\description{
Low-level pure-function entry point to the MOON Markov cohort engine.
Most users should call \code{\link[=moon_deterministic]{moon_deterministic()}} instead, which builds and
validates the inputs and returns tidy output. \code{run_markov_engine()} is
exported for advanced sensitivity-analysis workflows that need direct
access to the engine's six-state trace and per-capita cost matrix.
}
\details{
The engine is pure: identical inputs produce byte-identical outputs.
No I/O, no global state, no \code{set.seed()} calls.
}
\examples{
\donttest{
params <- moon_params_norway(sex = "female")
out <- run_markov_engine(
  start_age        = params$start_age,
  max_age          = params$max_age,
  discount_rate    = params$discount_rate,
  init_prev        = params$init_prev,
  transition_probs = params$transition_probs,
  qx               = params$qx,
  mortality_hr     = params$mortality_hr,
  cost_df          = params$cost_df
)
dim(out$trace)
}

}
\seealso{
\code{\link[=moon_deterministic]{moon_deterministic()}} for the validated-input wrapper most
users want.
}
\keyword{internal}
