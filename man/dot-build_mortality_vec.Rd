% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-engine.R
\name{.build_mortality_vec}
\alias{.build_mortality_vec}
\title{Build state-specific mortality probability vectors from baseline \code{qx} and HRs}
\usage{
.build_mortality_vec(qx, mortality_hr, ages)
}
\arguments{
\item{qx}{Named numeric of NW mortality probabilities; names are ages
as character.}

\item{mortality_hr}{Data frame with columns \code{age_lower}, \code{OW}, \code{OB1},
\code{OB2} (three rows).}

\item{ages}{Integer vector of model ages to process.}
}
\value{
List with elements \code{NW}, \code{OW}, \code{OB1}, \code{OB2}, each a numeric
vector the length of \code{ages}.
}
\description{
Applies age-band-specific mortality hazard ratios to the baseline NW
mortality vector \code{qx}. Banding: \code{age < 35} uses HR = 1;
\verb{35 <= age < 50} uses the row where \code{age_lower == 35};
\verb{50 <= age < 70} uses \code{age_lower == 50};
\code{age >= 70} uses \code{age_lower == 70} (the same band is used above age 89).
}
\keyword{internal}
