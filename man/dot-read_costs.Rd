% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-data.R
\name{.read_costs}
\alias{.read_costs}
\title{Read a per-capita cost CSV into a tidy \verb{(age, state, cost)} data frame}
\usage{
.read_costs(data_dir, sex_code, with_se = FALSE)
}
\arguments{
\item{data_dir}{Directory containing the CSV.}

\item{sex_code}{\code{"F"}, \code{"M"}, or \code{"Both"}.}

\item{with_se}{If \code{TRUE}, also return the \code{Cost_SE} column as \code{cost_se}.}
}
\value{
Data frame with columns \code{age}, \code{state}, \code{cost} (and \code{cost_se}
if \code{with_se = TRUE}).
}
\description{
Renames the source file's \code{State == "N"} to \code{"NW"} so output uses the
engine's state vocabulary. Costs for ages 2–19 (zeros) and 81–100
(constant at the age-80 value) come straight from the source file — no
engine-side imputation. Drops NOK columns and trailing empty columns by
selecting only the columns we need.
}
\keyword{internal}
