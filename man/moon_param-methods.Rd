% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moon-params.R
\name{moon_param-methods}
\alias{moon_param-methods}
\alias{moon_param_value}
\alias{moon_param_sample}
\title{MOON parameter spec class system}
\usage{
moon_param_value(x, ...)

moon_param_sample(x, n, ...)
}
\arguments{
\item{x}{A spec object inheriting from \code{moon_param}.}

\item{...}{Additional arguments. Lognormal methods accept \code{z} (a
length-\code{n} \code{rnorm()} vector); gamma methods accept \code{u} (a length-\code{n}
\code{runif()} vector); both default to \code{NULL}.}

\item{n}{Integer; number of random draws to generate.}
}
\value{
\code{moon_param_value()} returns a numeric (vector or scalar
depending on the spec). \code{moon_param_sample()} returns a length-\code{n}
vector for scalar specs, or an \code{n}-row matrix for vector-valued specs.
}
\description{
S3 generics dispatched on \verb{moon_param_*} spec classes.
\code{moon_param_value()} returns the deterministic point value of a spec —
used internally as a safety net when a spec is encountered in a
deterministic context. \code{moon_param_sample()} returns \code{n} random draws and
accepts auxiliary shared-randomness vectors (\code{z} for lognormal HRs, \code{u}
for gamma costs) so \code{\link[=moon_sample_params]{moon_sample_params()}} can reuse one set of random
numbers across multiple specs to reproduce the published MOON
"one-world" convention.
}
\details{
When the auxiliary argument is \code{NULL} (the default) each spec generates
its own independent draws.
}
\seealso{
the constructors: \code{\link[=moon_param_fixed]{moon_param_fixed()}}, \code{\link[=moon_param_lognormal]{moon_param_lognormal()}},
\code{\link[=moon_param_gamma]{moon_param_gamma()}}, \code{\link[=moon_param_mvnorm]{moon_param_mvnorm()}}, \code{\link[=moon_param_dirichlet]{moon_param_dirichlet()}}.
}
