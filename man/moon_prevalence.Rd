% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moon-deterministic-extractors.R
\name{moon_prevalence}
\alias{moon_prevalence}
\title{Compute prevalence by age and state from a deterministic run}
\usage{
moon_prevalence(
  x,
  denominator = c("alive", "initial"),
  ages = NULL,
  by_sex = FALSE
)
}
\arguments{
\item{x}{A \code{\link[=moon_deterministic]{moon_deterministic()}} object.}

\item{denominator}{\code{"alive"} (default) or \code{"initial"}.}

\item{ages}{Optional integer vector to filter; \code{NULL} (default) returns
all ages.}

\item{by_sex}{\code{FALSE} (default) drops the \code{sex} column from the output;
\code{TRUE} keeps it. Each \code{moon_deterministic} object is single-sex by
construction, so this only matters for downstream multi-sex stitched
objects.}
}
\value{
A long data frame with columns \code{age}, optionally \code{sex},
\code{state}, and \code{prevalence}.
}
\description{
Returns a long data frame with one row per \verb{(age, [sex,] state)} and a
\code{prevalence} column. The state set carries the engine's six-state space
(\code{N_always}, \code{N_prev}, \code{OW}, \code{OB1}, \code{OB2}, \code{dead}).
}
\details{
Choice of denominator:
\itemize{
\item \code{"alive"} — denominator is the per-age sum of head-counts over alive
states. Prevalence sums to 1 per age. The \code{dead} state is dropped from
the output.
\item \code{"initial"} — denominator is \code{params$cohort_n} (constant). Prevalence
sums to 1 per age \strong{once the \code{dead} row is included}, since \code{dead}
drains the live ones.
}
}
\examples{
\donttest{
params <- moon_params_norway(sex = "female")
run    <- moon_deterministic(params)
head(moon_prevalence(run, ages = 40:45))
}

}
\seealso{
\code{\link[=moon_deterministic]{moon_deterministic()}}, \code{\link[=moon_costs]{moon_costs()}}.
}
