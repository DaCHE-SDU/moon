# Test whether an object is a MOON parameter spec

Test whether an object is a MOON parameter spec

## Usage

``` r
is_moon_param(x)
```

## Arguments

- x:

  Object to test.

## Value

`TRUE` if `x` inherits from class `"moon_param"`, otherwise `FALSE`.
