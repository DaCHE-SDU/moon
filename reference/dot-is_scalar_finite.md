# Test whether `x` is a finite length-1 numeric

Test whether `x` is a finite length-1 numeric

## Usage

``` r
.is_scalar_finite(x)
```

## Arguments

- x:

  Object to test.

## Value

`TRUE` / `FALSE`.
