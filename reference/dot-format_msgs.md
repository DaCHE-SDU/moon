# Format the moon_check_params message list into a single string

Format the moon_check_params message list into a single string

## Usage

``` r
.format_msgs(msgs)
```

## Arguments

- msgs:

  Character vector of problem messages.

## Value

Length-1 character.
